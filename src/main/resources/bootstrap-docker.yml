

spring:
  cloud:
    config:
      uri: http://config-server:8888
      fail-fast: true
      username: root
      password: pr0y3ct1

eureka:
  client:
    service-url:
      defaultZone: http://registry-server:8761/eureka/
    prefer-ip-address: true